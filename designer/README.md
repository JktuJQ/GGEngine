Designer
========

### CMakeLists
1. Change path to Qt compiler to one you are using *(Qt must be version 5.9.9 or higher)*.
2. GGEngine library file (.a) is located at "*data/library*" and CMakeLists uses this 
location, but you can recompile library file and set it's new location in CMakeLists.

--------
This C++20 executable was created at ** **, by **JktuJQ**.